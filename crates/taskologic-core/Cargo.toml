[package]
name = "taskologic-core"
description = "Taskologic's well... logic and brain."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[dependencies]
serde.workspace = true
chrono.workspace = true
chrono-tz.workspace = true
thiserror.workspace = true

[dev-dependencies]
# Only the prefs upgrade test needs it, and that one has to go through real
# JSON to be worth anything.
serde_json.workspace = true

[features]
# Exposes the fixture builders in board::test_support and task::test_support
# to other crates' tests.
test-support = []

[lints]
workspace = true
